from activity23 import sum
from code_challenge1 import asteris as ast

sum(28,2) #the function sum

#use help(sum) function to execute the docstring

ast() #should be asteris but since I declare it as 'ast', it must be executed as ast()
